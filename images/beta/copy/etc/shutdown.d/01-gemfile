#!/bin/bash

if [[ -f /srv/jekyll/Gemfile.docker ]]
then
  echo "Moving Gemfile.docker back to Gemfile"
  echo "I hope you did not have a Gemfile actually called that."
  mv /srv/jekyll/Gemfile.docker /srv/jekyll/Gemfile
fi
