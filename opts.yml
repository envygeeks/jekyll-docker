maintainer: Jekyll Core Team <hello@jekyllrb.com>
user: jekyll

#

stable_major_version: &stable_major_version "3"
stable_minor_version: &stable_minor_version "3.1"
stable_version: &stable_version "3.1.3"
pages_version: &pages_version "3.0.5"

#

versions:
  tag:
    *pages_version: *pages_version
    *stable_version: *stable_version
    pages: *pages_version

#

tags:
  *pages_version: old
  *stable_version: stable
  pages: old

#

aliases:
  latest: *stable_version
  stable: *stable_version
  *stable_minor_version: *stable_version
  *stable_major_version: *stable_version

#

development_packages:
  all:
    - zlib-dev
    - build-base
    - libxml2-dev
    - libxslt-dev
    - readline-dev
    - libffi-dev
    - ruby-dev
    - yaml-dev
    - zlib-dev
    - libxslt-dev
    - readline-dev
    - libxml2-dev
    - libffi-dev
    - ruby-dev
    - yaml-dev

#

packages:
  all:
    - zlib
    - libxml2
    - build-base
    - ruby-io-console
    - readline
    - libxslt
    - ruby
    - yaml
    - libffi
    - nodejs
    - ruby-irb
    - ruby-json
    - ruby-rake
    - ruby-rdoc
    - git

#

gems:
  tag:
    *stable_version: &stable_gems
      - html-proofer
      - jekyll-sitemap
      - jekyll-mentions
      - jekyll-coffeescript
      - jekyll-sass-converter
      - jekyll-redirect-from
      - jekyll-paginate
      - jekyll-compose
      - jekyll-feed
      - RedCloth
      - kramdown
      - jemoji
    pages:
      - github-pages
      - jekyll-github-metadata
    *pages_version: *stable_gems

#

env:
  all:
    LANGUAGE: en_US
    LANG: en_US.UTF-8
    JEKYLL_ENV: development
    LC_ALL: en_US
